//1
function absoluteValue(num){
     if (Number.isInteger(num) && num < 0)
  {return num * -1}
  
   else if (Number.isInteger(num))
  {return num}
 
  else if (num != Number.isInteger(num))
  {return "Null"}
}

console.log(absoluteValue("diamond"));

//Class solution

//
//if (typeof (int)==="number") Type of will check to see if the variable is an integer.
//    
//



